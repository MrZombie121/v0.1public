
# SkyWatch UA - Система мониторинга воздушных целей 🛰️

MVP системы для визуализации направлений движения воздушных целей над Украиной.

## 🚀 Варианты запуска

### Способ 1: Скрапер (Рекомендуется, если не работает API)
Этот метод **не требует** регистрации на `my.telegram.org` и ввода номера телефона. Он просто читает публичные веб-страницы каналов.
1. Заполните в `.env` только `API_KEY` (Gemini).
2. Запустите: `npm run scraper` (или `start-all.bat`, он подхватит).

### Способ 2: MTProto Parser (Официальный)
Требует `TG_API_ID` и `TG_API_HASH`. Работает быстрее (push-уведомления), но сложнее в настройке.
1. Запустите: `npm run parser`.

## 🛠️ Структура проекта
- `server.js` — Бэкенд (API), принимает данные и обрабатывает их через Gemini AI.
- `scraper.js` — **NEW!** Читает Telegram через веб без авторизации.
- `parser.js` — Слушает Telegram через официальный протокол.
- `App.tsx` — Фронтенд (Карта).

## ❓ Решение проблем
Если вы видите `API_ID_INVALID` в `parser.js` — просто переходите на `scraper.js`. Результат будет тот же!
